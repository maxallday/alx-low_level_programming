#include "main.h"

/**
 * largest_number - returns the largest of 3 numbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */
if (a >= b && a >= c) 
{
    largest = a;
}
else if (b >= a && b >= c) /*Change > to >=*/
{
    largest = b;
}
else
{
    largest = c;
}

return (largest);

}
